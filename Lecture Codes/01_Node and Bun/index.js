// Use node filename.js or bun filename.js to run this file

// Function to add two numbers and return the sum
function sum(a, b) {
    return a + b; // Fixed: Added + operator
}

// Calling the function and storing the result in a variable 'ans'
const ans = sum(1, 2); // Fixed: Added '=' assignment operator

// Printing the result to the console
console.log(ans); // Expected output: 3